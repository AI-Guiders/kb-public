# Playbook: мультипроектный workspace и рациональный контекст (v1)

**Назначение:** чтобы агент не смешивал репозитории и не расходовал контекст на лишнее при работе в домашнем workspace с множеством `.sln`, MCP-серверов и карточек в каноне.

**Среда-независимость:** протокол живёт в `knowledge/`; применим в любой среде с доступом к канону (MCP agent-notes, клон репо, экспорт). Не требует Cursor rules.

**Связь:**
- `knowledge/playbook-clarification-general-query-v1.md` — если запрос размыт и не ясен даже **какой** проект затронут.
- `knowledge/playbook-captain-parallel-agents-v1.md` — параллельные субагенты (Task), капитан/воркер, тяжёлая разведка по репо.
- `knowledge/agent-memory-and-operating-principles-v1.md` — route_context, нечёткий поиск, один шаг до финала.
- Хаб workspace: `knowledge/work/projects/door-to-singularity/door-to-singularity/README.md`.
- Реестр решений: `knowledge/work/projects/door-to-singularity/solutions-registry.md`.

---

## 1. Триггеры (когда явно открывать этот плейбук)

- Пользователь или задача затрагивают **несколько** каталогов/`Financial/software/open/*` без явного «только этот репо».
- Вопросы вида: «куда смотреть», «что за проекты», «не путай X и Y», «какой контекст грузить».
- Риск **перечислить** все MCP или тащить в ответ несвязанные репо.

---

## 2. Первичный якорь: один `project-id` (или явное «нет продукта»)

**Перед** длинным чтением файлов:

1. Определить **primary** трек: стабильный **`project-id`** из дерева `knowledge/work/projects/door-to-singularity/<project-id>/` **или** явная пометка «задача уровня workspace / только реестр / только идеи».
2. Если primary неясен — **один** уточняющий вопрос или переформулировка («понимаю так: сейчас речь про …»), совместно с `route_context` по сырому запросу (см. clarification playbook).
3. **Не** подмешивать описания других продуктов, если нет зависимости (интеграция, общий протокол, общий submodule).

---

## 3. Порядок навигации по канону (узко → широко)

| Шаг | Что открыть | Зачем |
|-----|-------------|--------|
| A | `door-to-singularity/door-to-singularity/README.md` | Таблица: какие `project-id` заведены как карточки. |
| B | `door-to-singularity/<project-id>/README.md` | Длинный контур, backlog, связи — **если** карточка есть. |
| C | `solutions-registry.md` | Список `.sln`/`.slnx` и строка «канон — или нет». |
| D | README в корне целевого репо в workspace | Локальная сборка, команды, детали кода. |

**Правило:** не начинать с перечисления всех строк реестра в ответе пользователю; достаточно **текущего** primary и при необходимости одной ссылки «полный список — в solutions-registry».

---

## 4. Что не считать отдельным «миром» без запроса

- Репозитории, у которых в реестре в колонке канона стоит **«—»**, **не** тянуть в повествование, пока задача явно их не касается.
- **Кластеры** (например, несколько MCP + shared-библиотека): трактовать как **одну** тему до появления отдельной карточки в каноне; не распылять внимание на N почти одинаковых описаний.

---

## 5. Hot-context vs длинный KB

- **Hot / route_context:** только то, что нужно для **текущего** шага и primary `project-id`.
- **Полные README карточек и kb:** когда сменили primary, появился блокер по архитектуре/контракту между репо, или пользователь запросил глубину.

Не дублировать длинные блоки из канона в ответе целиком — **указатель + выжимка**.

---

## 6. Явные формулировки для пользователя (по желанию)

Короткие фразы улучшают сходимость: «сейчас primary: `cascade-ide`»; «остальные MCP в реестре не трогаю, если не скажешь».

**Маркеры в чате (как WORK/HUMAN):** см. секцию **`project-switch-protocol-v1`** в `agent-notes.md` — `[PRIMARY:<project-id>]`, `[SCOPE:door-to-singularity|portal|harvester|imc|mixed]`; дефолт из `workspace-scope-map-v1`, явный маркер в сообщении важнее. Для scope допустимы алиасы **`DTS`** и legacy **`current-projects`** → `door-to-singularity`; **`PTL`** → `portal`; **`HRV`** / **`EDWH`** → `harvester` (`project-ids-quickref-v1.md`, «Алиасы scope»).

**Алиасы `project-id`:** необязательные сокращения (`CIDE`, `AFL`, …) — таблица «Алиасы → канон» в `knowledge/work/projects/door-to-singularity/door-to-singularity/project-ids-quickref-v1.md`. Агент **нормализует** алиас к канону перед выбором карточки и путей; в долговечные записи в `knowledge/` писать только канонический id.

---

## 6b. Проактивные заметки агента (куда писать без явного запроса)

**Разрешено:** дополнять `agent-notes` (`append_agent_notes`) и точечно `knowledge/` (секции в карточках), когда это снимает повторяемую путаницу, фиксирует договорённость или экономит следующий сеанс — в духе микро-улучшений из `agent-memory-and-operating-principles-v1`.

**Выбор карточки / `project-id`:**
1. Маркеры в сообщении: `[PRIMARY:…]`, `[SCOPE:…]` (важнее дефолта из `workspace-scope-map-v1`).
2. Иначе `route_context` / `read_hot_context` и блок `active-scope` в заметках.
3. Иначе хаб `door-to-singularity/door-to-singularity/README.md` — таблица карточек и строка primary.
4. Если всё ещё неясно — **одна** короткая пометка в начале блока («предполагаю primary: …») **или** один уточняющий вопрос; не молча класть долгоживущие факты в чужую карточку.

**Куда класть по типу контента:**
- разовая операционная пометка — `append_agent_notes` или узкая секция в `agent-notes`;
- устойчивый факт про продукт — `knowledge/work/projects/door-to-singularity/<project-id>/` (README или отдельный kb-файл в той же карточке).

Стабильный Primary на хабе упрощает дефолт; смена secondary-проектов не ломает правило: новая тема — проверка таблицы / маркер / один вопрос.

---

## 7. Версия

v1.2. 2026-04-11. Источник: практика workspace PersonalCursorFolder + соглашение о scope `door-to-singularity` (бывш. `current-projects`). Обновление 2026-04: ссылка на `project-switch-protocol-v1`. Обновление 2026-04-02: §6b — проактивные заметки и маршрутизация в карточки. **Обновление 2026-04-11:** §6 — алиасы `project-id` и нормализация к канону (`project-ids-quickref-v1.md`).